AGENTS.md

# shadcn registry components. Prettier reformatting these makes every
# `shadcn add <name> --diff` report noise instead of real upstream changes, so
# the CLI's own formatting is left intact. Nothing hand-written belongs here —
# our own components live in src/components/common.
src/components/ui/**
