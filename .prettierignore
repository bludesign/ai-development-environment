AGENTS.md

# shadcn registry components. Prettier reformatting these makes every
# `shadcn add <name> --diff` report noise instead of real upstream changes, so
# the CLI's own formatting is left intact. The negated files below are ours,
# not the registry's, and stay formatted.
src/components/ui/**
!src/components/ui/date-time.tsx
!src/components/ui/detail-list.tsx
!src/components/ui/patch-view.tsx
!src/components/ui/searchable-select.tsx
!src/components/ui/*.test.tsx
